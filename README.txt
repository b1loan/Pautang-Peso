Pautang-Peso Website
Files created for the user. Drop these files into your GitHub Pages repo root or the 'docs' folder.
Live site: https://b1loan.github.io/Pautang-Peso/

Notes:
- Login uses localStorage (remember me).
- Images are referenced from your GitHub raw URLs and local 'files/' paths (you already have files in repo).
- QR code uses the provided raw GitHub URL.
- Verification screen stays for 10 seconds to allow screenshotting.
